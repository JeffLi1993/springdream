<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

       <!--======================================DAO层相关配置======================================-->
       <!-- scan package : 将标注Spring注解的类自动转化为Bean,IOC并完成Bean的注入 -->
       <context:component-scan base-package="org.jeff.spring.dao"/>

       <!-- DBCP实现的数据源 Bean -->
       <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
              destroy-method="close">
              <property name="driverClassName" value="com.mysql.jdbc.Driver"/>
              <property name="url" value="jdbc:mysql://localhost:3306/springdream_websample"/>
              <property name="username" value="root"/>
              <property name="password" value="123456"/>
       </bean>

       <!-- 定义一个JDBC模板 Bean -->
       <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
              <property name="dataSource" ref="dataSource"/>
       </bean>

       <!--======================================Service层相关配置======================================-->
       <!-- scan package:将service包下标注Spring注解的类转化为Bean,IOC并完成Bean的注入。 -->
       <context:component-scan base-package="org.jeff.spring.service"/>

       <!-- 配置事务管理器:负责声明式事务管理，引用了dataSource Bean -->
       <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
              <property name="dataSource" ref="dataSource"/>
       </bean>

       <!-- 通过AOP配置提供事务增强，让Service包下所有Bean的所有方法拥有事务 -->
       <aop:config proxy-target-class="true">
              <!-- AOP切入点 -->
              <aop:pointcut id="serviceMethod" expression="execution(* *..service..*.*(..))"/>
              <!-- Spring Advisor类似拦截器 -->
              <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceMethod"/>
       </aop:config>
       <!-- AOP通知 -->
       <tx:advice id="txAdvice" transaction-manager="transactionManager">
              <tx:attributes>
                     <tx:method name="*"/>
              </tx:attributes>
       </tx:advice>

</beans>